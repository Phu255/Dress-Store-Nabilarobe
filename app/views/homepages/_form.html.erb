<body class="homepageform">



<%= form_with(model: homepage) do |form| %>
  <% if homepage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(homepage.errors.count, "error") %> prohibited this homepage from being saved:</h2>

      <ul>
        <% homepage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
  <p>Video for homepage:</p>
  <video muted autoplay controls loop %>" >
    <source src="<%= url_for(homepage.video)%>" type="video/mp4" />
  </video>
  </div>

  <div>
    <p>Change video for homepage:</p>
    <%= form.file_field :video %>
  </div>


  <br>
  <div>
    <%= form.submit %>
  </div>
<% end %>



</body>